import React from 'react';

// Temporary placeholder component to resolve PostCSS error
// This file can be removed once the build system cache is cleared
const SearchFiltersDrawer: React.FC = () => {
  return null;
};

export default SearchFiltersDrawer; 